import AuthForm from "@/components/auth/AuthForm";

const SignInPage = () => {
  return (
    <main className='w-screen h-[94vh] flex flex-col items-center justify-center py-0 px-[5%] overflow-hidden'>
      <AuthForm />
    </main>
  );
};

export default SignInPage;
