import AuthForm from "@/components/auth/AuthForm";

const SignupPage = () => {
  return (
    <main className='w-screen h-screen flex flex-col items-center justify-center gap-4 py-0 px-[5%] overflow-hidden'>
      <AuthForm />
    </main>
  );
};

export default SignupPage;
